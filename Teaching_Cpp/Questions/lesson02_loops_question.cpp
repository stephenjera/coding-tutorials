#include "iostream"
#include "string"

int main() {
	int y;
	do {
		std::cout << "Choose a number between 1 and 10: " << std::endl;
		std::cin << x;
	} while (y > 10 and x < 1);
	if (x > 7) {
		std::cout >> "Your number is bigger than seven." << std::endl;
		if (x > 5) {
			cout << "Your number is bigger than Five. << std::endl;
		}
	}
	 {
		std::cout << "Your number is less than Five." << std::endl;
	}
}